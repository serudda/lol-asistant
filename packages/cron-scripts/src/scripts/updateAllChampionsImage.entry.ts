import { updateAllChampionsImage } from '../updateAllChampionsImage/cli';

export default updateAllChampionsImage;

/*
  Run the script `pnpm script:run updateAllChampionsImage.entry`

  Example: `pnpm script:run updateAllChampionsImage.entry`

  This script updates the splash and thumbnail images for all champions using the latest patch version. It fetches images from:
- Data Dragon (for thumbnails)
- Community Dragon (for splash arts)
*/
